1) Создать пользователя devops с домашней директорией и комментарием "DevOps Engineer"; установить пароль qwerty123 для пользователя devops
2) Создать группу docker и добавить в неё пользователя devops
3) Проверить принадлежность пользователя devops к группе docker
4) Создать директорию `/opt/srv/backups` с владельцем `devops:docker` и правами `770`
5) Создать директорию `/opt/srv/nginx`, установить владельца `root:www-data` и права `750`
6) Создать файлы
- `/opt/srv/nginx/nginx.conf`
- `/opt/srv/nginx/mime.types`
- `/opt/srv/nginx/backend.conf`
7) Рекурсивно изменить владельца всех файлов в `/opt/srv/nginx` на `www-data:devops` и права на `644`
8) Изменить UID пользователя `devops` на `1500`
9) Создать пользователя `gitlab-runner` с системным UID (без домашней директории)
10) Создать пользователя `jenkins`; задать пароль для только что созданного пользователя
11) Создать пользователя `logger` и сразу создать для него домашнюю директорию `/var/logger`
12) Создать пользователя `gitlab-runner`
13) Создать группу `deployers`, добавить в нее пользователей `jenkins` и `gitlab-runner`
14) Сменить владельца файла `/var/log/ecom_web.log` на `logger`
15) Сменить группу-владельца файла `/var/log/ecom_web.log` на `deployers`
16) Посмотреть UID, GID и группы пользователя `jenkins`
17) Создать системного пользователя `service_account` без создания домашней директории
18) Изучить механизм ACL
19) Создать директорию `/var/www/server-app`; в ней файлы `readme.md` и `app.log`; директорию `uploads`
20) Создать:
- группу `ftpusers`
- группу `admins`
- группу `auditors`
- пользователя `logger` с домашней директорией `/opt/logger` и без возможности входа в систему
21) Разрешить с помощью ACL:
- пользователю `www-data` чтение всех файлов
- группе `ftpusers` запись в `uploads`
- группе `admins` полный доступ 
- пользователю `logger` полный доступ только к файлу `app.log`
- группе `auditors` чтение файла `app.log`
22) Настроить систему так, чтобы новые файлы создавались с правами `rw-r-----`, а директории - `rwxr-x---`
> [!IMPORTANT]  
> не забудьте восстановить исходное состояние системы после проверки

23) Разрешить пользователю `logger` вход в систему; переключиться на него с помощью `su`, сохранив текущее окружение и создать файл `/tmp/logger-envs` и вывести в него переменные окружения. Выйти и переключиться еще раз с `-`; сравнить вывод `env` и содержимое файла `/tmp/logger-envs`
24) Без переключения пользователя создать от имени `logger` директорию `/tmp/logger-dir`